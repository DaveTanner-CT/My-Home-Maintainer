name: HomeKeeper

options:
  bundleIdPrefix: org.scriptingforschools

settings:
  base:
    IPHONEOS_DEPLOYMENT_TARGET: '17.0'

targets:
  HomeKeeper:
    type: application
    platform: iOS

    sources:
      - path: Models
      - path: Services
      - path: Utilities
      - path: Views
      - path: App

    resources:
      - path: Resources/Assets.xcassets

    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.scriptingforschools.HomeMaintainer
        PRODUCT_NAME: HomeKeeper
        SWIFT_VERSION: '5.0'
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        TARGETED_DEVICE_FAMILY: '1'
        INFOPLIST_KEY_CFBundleDisplayName: My Home Keeper
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: My Home Keeper lets you attach photos to home records and project ideas.
        INFOPLIST_KEY_NSCameraUsageDescription: My Home Keeper lets you photograph equipment labels, paint cans, receipts, and project ideas.
        INFOPLIST_KEY_NSContactsUsageDescription: My Home Keeper lets you import vendor details from Contacts and add vendors to Contacts.

    info:
      path: Info.plist
      properties:
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
